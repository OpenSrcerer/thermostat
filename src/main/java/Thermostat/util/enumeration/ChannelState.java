package thermostat.util.enumeration;

/**
 * An enum used for convenience in identifying channels that are
 * monitored / filtered.
 */
public enum ChannelState {
    UNMONITORED, UNFILTERED, MONITORED, FILTERED
}
